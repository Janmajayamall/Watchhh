import * as React from "react";
// import { createCeramic } from "./utils/ceramic";
// import { getProvider, metamask } from "./utils/wallet";

const App = () => {
	return (
		<div>
			<img src={require("./icon.png")} width={50} height={50} />
			<text>Archive your twitter on Decentralized Web</text>
		</div>
	);
};

export default App;
